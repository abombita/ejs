<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%> - <%= name%></title>
</head>
<body>
    <h1>EJS Template</h1>

    <%- include('partials/page2')%>

    <%- 10 + 5%>
    <%- 'Hello' + '' + 'World' %>
    <%= '<script>alert("hello")</script>' %>

    <% if (true) {%>
        <h2>This is neat</h2>
    <% } %>

    <% loggedIn = false; if (loggedIn) { %>
        <h3> You are logged in as <%- loggedIn %></h3>
    <% } else { %>
        <h3><a href= "/login">Please log in </a></h3>
    <% } %>

    <ul>
        <% for (i = 0; i > 10; i ++){ %>
            <li><%- i %></li>
        <% } %>
    </ul>
</body>
</html>